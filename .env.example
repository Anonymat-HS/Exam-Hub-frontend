# Copier en ".env" puis adapter. Ne jamais committer ".env".

# URL de l'API backend (build time). Exemples:
#   encore en dev      -> http://localhost:8080
#   backend déployé    -> https://exam-hub-api.onrender.com
VITE_API_URL=